<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.hospital.persistence.mybatis.DepartmentMapper">

    <resultMap id="DepartmentResultMap" type="Department">
        <id column="id" property="id"/>
        <result column="hospital_id" property="hospitalId"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
    </resultMap>

    <insert id="insert" parameterType="Department" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO departments (hospital_id, code, name, description)
        VALUES (#{hospitalId}, #{code}, #{name}, #{description})
    </insert>

    <select id="selectById" parameterType="long" resultMap="DepartmentResultMap">
        SELECT id, hospital_id, code, name, description
        FROM departments
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="DepartmentResultMap">
        SELECT id, hospital_id, code, name, description
        FROM departments
    </select>

    <select id="selectByHospitalId" parameterType="long" resultMap="DepartmentResultMap">
        SELECT id, hospital_id, code, name, description
        FROM departments
        WHERE hospital_id = #{hospitalId}
    </select>

    <update id="update" parameterType="Department">
        UPDATE departments
        SET hospital_id = #{hospitalId},
            code = #{code},
            name = #{name},
            description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM departments WHERE id = #{id}
    </delete>

</mapper>
