<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.hospital.persistence.mybatis.PatientMapper">

    <resultMap id="PatientResultMap" type="Patient" autoMapping="false">
        <id column="id" property="id"/>
        <result column="fullName" property="fullName"/>
        <result column="birthDate" property="birthDate"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
        <result column="insuranceId" property="insuranceId"/>
    </resultMap>

    <insert id="insert" parameterType="Patient" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO patients (full_name, birth_date, gender, phone, insurance_id)
        VALUES (#{fullName}, #{birthDate}, #{gender}, #{phone}, #{insuranceId})
    </insert>

    <select id="selectById" parameterType="long" resultMap="PatientResultMap">
        SELECT id,
               full_name AS fullName,
               birth_date AS birthDate,
               gender,
               phone,
               insurance_id AS insuranceId
        FROM patients
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="PatientResultMap">
        SELECT id,
               full_name AS fullName,
               birth_date AS birthDate,
               gender,
               phone,
               insurance_id AS insuranceId
        FROM patients
    </select>

    <select id="selectByInsurance" parameterType="long" resultMap="PatientResultMap">
        SELECT id,
               full_name AS fullName,
               birth_date AS birthDate,
               gender,
               phone,
               insurance_id AS insuranceId
        FROM patients
        WHERE insurance_id = #{insuranceId}
    </select>

    <update id="update" parameterType="Patient">
        UPDATE patients
        SET full_name = #{fullName},
            birth_date = #{birthDate},
            gender = #{gender},
            phone = #{phone},
            insurance_id = #{insuranceId}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM patients WHERE id = #{id}
    </delete>

</mapper>
